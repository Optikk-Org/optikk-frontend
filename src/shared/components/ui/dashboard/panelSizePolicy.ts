/** Pixel spacing for react-grid-layout; grid column count matches backend panel layout units. */
export const GRID_COLS = 12;
/** Row height in px (Datadog-like density; paired with backend layout `h` in grid units). */
export const GRID_ROW_HEIGHT = 36;
export const GRID_MARGIN: [number, number] = [8, 8];
export const GRID_CONTAINER_PADDING: [number, number] = [0, 0];
